name: Compile GBA
on: [push]
jobs:
 Compile-GBA:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v3
      - name: build docker image
      - run: make build_image